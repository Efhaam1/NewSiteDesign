# S115 — complete record from `C:\Users\MUS\Desktop\CurriculumWwebsitePrototype\app\data\showcase.json`

Bundle metadata: `bundle_version` `"1.1.0"`, `generated` `"2026-08-29T06:42:12.908Z"`, and at line 2 a do-not-edit marker: `"$comment": "GENERATED by scripts/build-showcase.ts — do not edit. Run \`npm run bundle:showcase\`."`. `data` holds exactly three keys: `["S001","S042","S115"]`. The S115 record starts at line 924 and serializes to 40,470 chars.

S115 top-level keys, in order: `schema_version`, `id`, `session_number`, `stage`, `level`, `unit`, `session_type`, `title`, `subtitle`, `learning_objective`, `expected_outcomes`, `prerequisites`, `concepts`, `thinking_routine`, `estimated_duration_min`, `age_band`, `delivery`, `materials_required`, `teaching_flow`, `puzzles`, `model_games`, `practical_activity`, `homework`, `coach_notes`, `tags`, `author`, `version`. `schema_version` is `"1.0"`, `author` is `"Lead Curriculum Author"`, `version` is `"1.0"`. `model_games` is `[]` (line 1357).

## 1. Identity fields

| field | value |
|---|---|
| `id` | `"S115"` |
| `session_number` | `115` (number) |
| `stage` | `{"number": 3, "name": "Bishop", "color": "Purple"}` |
| `level` | `{"code": "3B", "name": "The Strategic Player"}` |
| `unit` | `{"number": 5, "name": "Advanced Tactics & Sacrifice"}` |
| `title` | `"Three-Move Combinations"` |
| `subtitle` | `"Calculate the entire forced line to checkmate before you play move one."` |
| `learning_objective` | `"Students will calculate and execute forced three-move combinations by mapping every check and forced reply in the full line to mate before committing to move one."` |
| `age_band` | `"10-12"` (ASCII hyphen in the data — line 994) |
| `estimated_duration_min` | `60` (line 993) |
| `session_type` | `"teaching"` |

Adjacent fields you did not ask for but that the console reads: `prerequisites.sessions` = `["S112","S113","S114"]`; `prerequisites.concepts` = 8 items; `concepts.primary` = `"forced three-move combination"`, `concepts.secondary` = 8 items, `concepts.review` = 6 items; `expected_outcomes` = 4 items; `delivery` = `["in_person","online"]`; `materials_required` = 5 items; `tags` = 19 items beginning `"stage-3"`, `"bishop-stage"`, `"level-3B"`, `"unit-5"`, `"teaching"` and including `"the-plan"` and `"age-10-12"`.

## 2. teaching_flow, timings, and the "60 / 42" arithmetic

`teaching_flow` has exactly six keys, in this order:

| # | key | type | `duration_min` | sub-keys |
|---|---|---|---|---|
| 0 | `warmup_review` | object | **8** (line 1008) | `duration_min`, `description`, `review_items` (3 items, each `{revisits, fen, prompt, answer}`) |
| 1 | `lesson_introduction` | object | **5** (line 1032) | `duration_min`, `hook`, `narrative`, `description` |
| 2 | `core_explanation` | object | **7** (line 1038) | `duration_min`, `key_points` (5), `explanation`, `demonstrations` (`[]` — empty) |
| 3 | `guided_discussion` | object | **6** (line 1050) | `duration_min`, `description`, `talking_points` (5) |
| 4 | `questions_to_ask` | array of 6 strings | **none** | — |
| 5 | `guided_examples` | array of 2 objects | **none** | each `{fen, prompt, walkthrough, answer}` |

`practical_activity`: `name` = `"Calculate-Out-Loud Combination Game"`, `duration_min` = **16** (line 1362), `type` = `"guided_rapid"`, plus `learning_objective`, `setup`, `instructions` (4), `success_criteria`, `variations` (4).

`homework.estimated_time_min` = **15** (line 1394). Note the key is `estimated_time_min`, not `duration_min`, so nothing that sums `duration_min` picks it up.

A recursive walk of the whole S115 record finds **exactly five** `duration_min` fields and nothing else:

```
8   S115.teaching_flow.warmup_review.duration_min
5   S115.teaching_flow.lesson_introduction.duration_min
7   S115.teaching_flow.core_explanation.duration_min
6   S115.teaching_flow.guided_discussion.duration_min
16  S115.practical_activity.duration_min
```

**Sum = 42. Count = 5.**

Arithmetic: `8 + 5 + 7 + 6 = 26` (the four timed teaching_flow segments), `26 + 16 = 42` (plus the practical activity). Against `estimated_duration_min: 60` that leaves `60 − 42 = 18` minutes unaccounted for by any `duration_min` field. (If homework's 15 were folded in it would read 57, not 42 — but homework is a separate key name and happens at home.)

**Does "60 MIN PLANNED · 42 TIMED" hold?** The two *numbers* are correct for S115: 60 is `estimated_duration_min`, 42 is the sum of every `duration_min`. But no string in the app renders that pair. The console's plan-pane footer at `C:\Users\MUS\Desktop\CurriculumWwebsitePrototype\app\js\ui\console.js:148-149` prints the *count*, not the sum:

```js
el(foot, 'span', '', `${L.estimated_duration_min} min planned`);
el(foot, 'b', '', `${rows.length} timed parts`);
```

so for S115 the footer renders `60 min planned` + `5 timed parts`. The comment immediately above it (lines 143-147) says a ratio was deliberately avoided there. The number 42 appears only in the prep sheet, at `console.js:373-374`:

```js
['planned', `${L.estimated_duration_min} min`],
['timed', `${total} min`],
```

which for S115 renders `planned 60 min` / `timed 42 min`. `total` is `rows.reduce((a, r) => a + r[1], 0)` (`console.js:51`), and `rows` comes from `flowRows()` (`console.js:96-108`), which walks a fixed four-key `SEGMENTS` list then appends the practical activity — exactly the five fields above. So "60 planned, 42 timed" is a true statement about S115 that is currently rendered as two separate lines in two different places, never as one "60 · 42" string.

## 3. thinking_routine

Two keys only: `current` and `focus`.

`current` is `"THE PLAN"` — **all caps** (line 990). It is **not** `"The Plan"`. If a rebuild is matching or displaying the literal, the value is uppercase; the app prints it as-is (`el(r, 'p', 'r-v', L.thinking_routine.current)`, `console.js:129`), so it appears on screen as `THE PLAN`.

`focus` (785 chars), verbatim:

> `"Before touching a piece, students must trace the entire three-move forcing line in their head: identify every candidate check or capture on move one, then ask 'What can my opponent actually do?' for each reply — not what they hope the opponent plays, but every legal option. A move is truly forced only when the opponent has exactly one legal or reasonable response. Students work forward through all three plies (their move, opponent's reply, their follow-up) and confirm the position is checkmate at the end. Only once the whole line is spoken or visualised to its conclusion does a student commit to move one. This discipline — calculate to the end before committing — is the defining habit of Unit 5 and the skill students will carry forward through every advanced tactics session."`

## 4. puzzles — 8 of them

All eight share an identical 12-key shape: `id`, `fen`, `side_to_move`, `prompt`, `solution`, `explanation`, `educational_purpose`, `difficulty`, `difficulty_stars`, `theme`, `common_mistakes`, `hint`. `solution` is always `{moves, san_line, result}`.

**There is no `tags`, `category`, or `type` field on any puzzle.** The nearest equivalents are `theme` (array of strings) and `difficulty` (string label: `"Foundation"` / `"Core"` / `"Challenge"`). `tags` exists only at lesson level; `type` exists only as lesson-level `session_type` and `practical_activity.type`.

Stars run `[1,1,2,2,2,3,3,3]`; every `solution.moves` is 5 entries long (three of the mover's moves plus two forced replies) and every `solution.result` is `"Checkmate."`.

### Index 0 — `"S115-P1"` (POSITION 1 OF 8)
- `fen`: `"6k1/p4ppp/2R5/1p4P1/3q4/7P/1N2r3/1K6 w - - 1 48"`
- `side_to_move`: `"w"` · `difficulty`: `"Foundation"` · `difficulty_stars`: `1`
- `theme`: `["mate in three","back-rank mate","rook ladder","forcing moves","calculation","interposition"]`
- `prompt`: `"White has two rooks aimed at the back rank and the enemy queen is the only defender. The plan is a rook ladder: a check that forces the queen to block, a capture that forces the queen to block again, and a final capture that ends the game. Calculate the full forcing sequence to the end before you move. Mate in three."`
- `solution.moves`: `["Rc8+","Qd8","Rxd8+","Re8","Rxe8#"]`
- `solution.san_line`: `"1.Rc8+ Qd8 2.Rxd8+ Re8 3.Rxe8#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1. Rc8+ forces the queen to block on d8 — the king has nowhere to go and the queen must interpose. 2. Rxd8+ captures that blocker with check, forcing the queen to interpose again on e8 — it is the only legal way to meet the check. 3. Rxe8# delivers mate: the rook on e8 is protected, the king cannot move, and no piece can capture or block. The queen is gone, exhausted by two forced interpositions."`
- `educational_purpose`: `"Teaches students to calculate a repeating check-force-capture pattern (rook ladder) all the way to the final mating position before committing to move one."`
- `common_mistakes` (2): `"Capturing the queen immediately with the wrong rook, releasing the back-rank pressure and letting the king escape."` / `"Stopping after the first or second capture and not verifying that the final position is actually checkmate."`
- `hint`: `"Think of your two rooks as relay runners — each check hands the baton to the other by forcing a new block on the same rank."`

### Index 1 — `"S115-P2"` (POSITION 2 OF 8)
- `fen`: `"2Q5/p4pk1/3p2pp/8/3RP3/5P2/1r4PP/7K b - - 0 30"`
- `side_to_move`: `"b"` · `difficulty`: `"Foundation"` · `difficulty_stars`: `1`
- `theme`: `["mate in three","first-rank mate","rook ladder","forcing moves","calculation","interposition"]`
- `prompt`: `"Black's two rooks are lined up against the first rank and White's queen is the only piece standing between them and victory. The mechanism is the same as the classic rook ladder, but now Black is the one executing it. Calculate every forced reply before you commit. Mate in three."`
- `solution.moves`: `["Rb1+","Qc1","Rxc1+","Rd1","Rxd1#"]`
- `solution.san_line`: `"1.Rb1+ Qc1 2.Rxc1+ Rd1 3.Rxd1#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1...Rb1+ forces the white queen to block on c1 — the king is trapped on the first rank and interposition is the only legal reply. 2...Rxc1+ captures that blocker with check, forcing the queen to interpose again on d1. 3...Rxd1# delivers mate: the king cannot move, the rook on d1 is protected by its partner, and the queen has been consumed by two consecutive forced blocks."`
- `educational_purpose`: `"Reinforces the rook-ladder pattern from the other side of the board, training students to recognise the mechanism regardless of which color is attacking."`
- `common_mistakes` (2): `"Playing the checks in reverse order, which gives the queen a chance to escape the ladder and survive."` / `"Assuming the position is a draw because Black is moving first and overlooking that both rooks are already on the critical rank."`
- `hint`: `"The queen must keep blocking, but each block puts her on a square where she can be captured — trace the queen's forced journey from square to square."`

### Index 2 — `"S115-P3"` (POSITION 3 OF 8)
- `fen`: `"1k3r2/pp6/3p3p/8/5q2/2Q5/P4P2/K1R5 w - - 1 32"`
- `side_to_move`: `"w"` · `difficulty`: `"Core"` · `difficulty_stars`: `2`
- `theme`: `["mate in three","queen sacrifice","deflection","back-rank mate","calculation","forcing moves"]`
- `prompt`: `"White's queen and rook are both bearing down on the queenside, and the enemy king is trapped in the corner with only a rook guarding the back rank. The plan involves giving up the queen to strip that rook from its defensive post — a true sacrifice that must be calculated to the very end. Mate in three."`
- `solution.moves`: `["Qc7+","Ka8","Qc8+","Rxc8","Rxc8#"]`
- `solution.san_line`: `"1.Qc7+ Ka8 2.Qc8+ Rxc8 3.Rxc8#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1. Qc7+ forces the king to a8 — it is the only square available in the corner. 2. Qc8+! sacrifices the queen on c8, deflecting the rook: the rook must capture on c8 because it is the only legal interposition. 3. Rxc8# — the white rook recaptures with check and the king is mated in the corner; it cannot move and there is nothing left to block or capture."`
- `educational_purpose`: `"Teaches the queen-sacrifice deflection pattern: giving up the most powerful piece to remove the one defender, a calculation that requires seeing three moves deep before the sacrifice makes sense."`
- `common_mistakes` (2): `"Hesitating to sacrifice the queen because it feels wrong to give up so much material, and instead playing a quiet move that gives the opponent time to escape."` / `"Playing the rook check first without driving the king to a8, which leaves the king an escape square and fails the combination."`
- `hint`: `"Ask yourself: if you could remove the rook from c8's defence, what would happen? Then find the forcing move that makes the rook leave."`

### Index 3 — `"S115-P4"` (POSITION 4 OF 8) — both of your specifics confirmed
- `fen`: `"8/p3k3/1q2rpp1/8/1p2R3/8/PPP1Q2P/1K6 b - - 1 30"` — **exact match** to the string you gave, character for character (showcase.json line 1188).
- `prompt` (line 1190) — **starts exactly** `"Black has a queen and rook aimed at the e-file"`. Full value: `"Black has a queen and rook aimed at the e-file, and White's queen and rook are both stationed there — but Black can use the x-ray relationship between them. The plan is to force White's queen onto a square where Black's rook will already be pointing straight through it. Calculate the battery all the way to checkmate before you move. Mate in three."`
- `side_to_move`: `"b"` · `difficulty`: `"Core"` · `difficulty_stars`: `2`
- `theme`: `["mate in three","x-ray","battery","e-file","calculation","forcing moves"]`
- `solution.moves`: `["Qg1+","Qe1","Qxe1+","Rxe1","Rxe1#"]`
- `solution.san_line`: `"1.Qg1+ Qe1 2.Qxe1+ Rxe1 3.Rxe1#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1...Qg1+ forces the white queen to block on e1 — the king must not be exposed and interposition is the only legal reply. 2...Qxe1+ captures the blocking queen with check, forcing the white rook to recapture on e1 — it is the only way to answer the check. 3...Rxe1# — the black rook slides in behind and delivers mate; the king cannot move and no piece can interpose."`
- `educational_purpose`: `"Trains students to see the x-ray battery — two pieces on the same line separated by a defender — and calculate how forcing moves can clear the middle piece and let the back piece deliver mate."`
- `common_mistakes` (2): `"Moving the rook first instead of the queen, breaking the x-ray relationship and throwing away the forced sequence."` / `"Not noticing that after the queen exchanges, the rook is already pointing at the king with no pieces in the way."`
- `hint`: `"Picture your rook waiting behind your queen — if the queen trades herself off, what is the rook looking at?"`

The "POSITION 4 OF 8" label is produced by the teach page, not the console: `app/js/teach/segments.js:74` maps `lesson.puzzles` 1:1 onto that segment's beats, and `app/js/teach/main.js:353` renders `position ${S.beat + 1} of ${sg.beats.length}`. So POSITION 4 OF 8 is unambiguously `puzzles[3]` = `S115-P4`.

### Index 4 — `"S115-P5"` (POSITION 5 OF 8)
- `fen`: `"4r2k/p6p/3q2p1/2b5/2Q5/PB6/1P4PP/3R3K b - - 2 31"`
- `side_to_move`: `"b"` · `difficulty`: `"Core"` · `difficulty_stars`: `2`
- `theme`: `["mate in three","deflection","queen sacrifice","first-rank mate","calculation","forcing moves"]`
- `prompt`: `"Black's rook is on the e-file and the white queen is the only piece blocking a check from reaching the king. The plan begins with a shocking queen sacrifice that pulls the white bishop away from its defensive square, opening a forcing rook check that cannot be answered. Calculate all three moves before you commit — the sacrifice only works if the finale is forced. Mate in three."`
- `solution.moves`: `["Qxd1+","Bxd1","Re1+","Qf1","Rxf1#"]`
- `solution.san_line`: `"1.Qxd1+ Bxd1 2.Re1+ Qf1 3.Rxf1#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1...Qxd1+! sacrifices the queen, forcing the bishop to recapture on d1 — it is the only legal way to answer the check. 2...Re1+ drives a rook check that forces the white queen to block on f1 — the king has no escape. 3...Rxf1# — the rook captures the blocking queen and the king is mated; it cannot move, capture (the rook is protected), or interpose."`
- `educational_purpose`: `"Teaches the deflection sacrifice: trading the queen to drag a key defender off its post, then executing a forcing rook sequence that the opponent is powerless to stop."`
- `common_mistakes` (2): `"Playing the rook check before sacrificing the queen, which allows White to meet the check without losing the queen and the combination collapses."` / `"Seeing the queen sacrifice but not calculating to the end and missing that the final rook check is met by a block that the rook can then capture with mate."`
- `hint`: `"The bishop on d1 is holding everything together — what is the most forceful way to make it move?"`

### Index 5 — `"S115-P6"` (POSITION 6 OF 8)
- `fen`: `"8/2R3pk/6pp/p7/1p4N1/1n1r3P/1P4P1/7K w - - 0 35"`
- `side_to_move`: `"w"` · `difficulty`: `"Challenge"` · `difficulty_stars`: `3`
- `theme`: `["mate in three","corner mate","knight fork","rook","calculation","forcing moves"]`
- `prompt`: `"White has a knight that can deliver check and a rook ready on the c-file. The plan is to use the knight to chase the king into the corner, then bring the rook crashing in to force a block, and finally deliver a corner mate. Every move is a check — calculate all three before you start. Mate in three."`
- `solution.moves`: `["Nf6+","Kh8","Rc8+","Rd8","Rxd8#"]`
- `solution.san_line`: `"1.Nf6+ Kh8 2.Rc8+ Rd8 3.Rxd8#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1. Nf6+ forces the king to h8 — it is driven into the corner with no alternative. 2. Rc8+ then delivers a rook check on the back rank, forcing the rook to block on d8 — it is the only legal interposition. 3. Rxd8# captures the blocking rook and delivers mate in the corner: the king is boxed in by its own pieces and cannot move."`
- `educational_purpose`: `"Teaches the knight-and-rook coordination pattern: use the knight check to steer the king to its worst square, then bring the rook in to finish with a back-rank sequence."`
- `common_mistakes` (2): `"Playing the rook check before the knight check, which does not drive the king to the corner and gives the opponent a way to escape the follow-up."` / `"After the king goes to h8, forgetting to check with the rook before capturing and allowing the king to slip away."`
- `hint`: `"The knight can act as a shepherd — which check sends the king exactly where you want it, with no way back?"`

### Index 6 — `"S115-P7"` (POSITION 7 OF 8)
- `fen`: `"6k1/R1p2ppp/3b4/4pN2/4n2P/6P1/P1r2PK1/8 w - - 1 29"`
- `side_to_move`: `"w"` · `difficulty`: `"Challenge"` · `difficulty_stars`: `3`
- `theme`: `["mate in three","deflection","pin","back-rank mate","calculation","forcing moves"]`
- `prompt`: `"White's rook can deliver a check that forces a piece to block — and that blocking piece will immediately be pinned against its own king by the knight that follows. The plan layers a deflection, a pin, and a capture into three consecutive forcing moves. Calculate the full sequence before you move. Mate in three."`
- `solution.moves`: `["Ra8+","Bf8","Ne7+","Kh8","Rxf8#"]`
- `solution.san_line`: `"1.Ra8+ Bf8 2.Ne7+ Kh8 3.Rxf8#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1. Ra8+ forces the bishop to block on f8 — it is the only way to interpose on the back rank. 2. Ne7+ checks the king and drives it to h8, while simultaneously pinning the f8 bishop to the king — the bishop cannot move to capture or block. 3. Rxf8# — the rook captures the pinned bishop with mate: the king is in the corner, the bishop is pinned and cannot move, and there are no other escape squares."`
- `educational_purpose`: `"Teaches students to combine a deflection move with a pin in a three-move sequence, calculating how one forcing move can set up a pin that makes the final capture unstoppable."`
- `common_mistakes` (2): `"Playing the knight check before the rook check, which does not place a pinned piece on f8 and leaves the king with a way to avoid mate."` / `"Not recognising that the bishop on f8 will be absolutely pinned after the knight check, and therefore thinking the combination fails because the bishop could capture."`
- `hint`: `"After the first rook check forces a block, ask yourself: which piece can deliver a check that also freezes that blocker in place?"`

### Index 7 — `"S115-P8"` (POSITION 8 OF 8)
- `fen`: `"7k/p5rP/1p3n2/5b2/8/1KPR4/P4q2/7R w - - 10 45"`
- `side_to_move`: `"w"` · `difficulty`: `"Challenge"` · `difficulty_stars`: `3`
- `theme`: `["mate in three","promotion","back-rank mate","forcing moves","calculation","rook sacrifice"]`
- `prompt`: `"White has a rook and a pawn on the seventh rank, and the enemy rook is the only piece that can try to hold the back rank. The plan forces the enemy rook to block two consecutive checks, and on the third move a pawn promotes — delivering checkmate on the spot. Calculate every forced reply before you push. Mate in three."`
- `solution.moves`: `["Rd8+","Ne8","Rxe8+","Rg8","hxg8=Q#"]`
- `solution.san_line`: `"1.Rd8+ Ne8 2.Rxe8+ Rg8 3.hxg8=Q#"` · `solution.result`: `"Checkmate."`
- `explanation`: `"1. Rd8+ forces the knight to block on e8 — it is the only legal interposition. 2. Rxe8+ captures the knight with check, forcing the rook to block on g8 — the king is trapped and interposition is the only reply. 3. hxg8=Q# — the pawn promotes to a queen with check and it is checkmate: the king is hemmed in, the newly promoted queen controls the escape squares, and there is no capture or block available."`
- `educational_purpose`: `"Teaches students to calculate a forcing sequence that ends with a pawn promotion to checkmate, combining back-rank tactics with the transformative power of promotion."`
- `common_mistakes` (2): `"Promoting the pawn immediately without first clearing the back rank with rook checks, which allows the opponent to capture the new queen."` / `"After the first rook check and block, exchanging rooks voluntarily instead of capturing the blocking piece with check to keep the sequence forcing."`
- `hint`: `"Your pawn is one step from promotion, but the back rank is guarded — can two rook checks clear the path and force a block exactly where the promoted queen will deliver mate?"`

Note on the promotion move: `"hxg8=Q#"` is the only SAN token in S115 that is not a plain piece move — worth flagging because the console replays `P.solution.moves` through `chess.move(m)` (`console.js:66-70`) to build board frames, and this puzzle is index 7, so the console (which only ever shows `puzzles[0]`) never touches it, but the teach page does.

## 5. coach_notes

Six keys, in order — five arrays and one string:

| key | items |
|---|---|
| `common_misconceptions` | 3 |
| `typical_mistakes` | 3 |
| `coaching_tips` | 4 |
| `ways_to_simplify` | 3 |
| `extensions_for_strong_students` | 3 |
| `pacing_notes` | not a list — a single string, 1,136 chars |

`pacing_notes` opens `"This session opens Unit 5 'Advanced Tactics and Sacrifice', so its primary goal is establishing THE PLAN as a non-negotiable habit, not maximising puzzle throughput."` and names the puzzle tiers explicitly: P1–P2 Foundation, P3–P5 Core, P6–P8 Challenge, matching the `difficulty` values above. Two `coaching_tips` reference specific puzzles by number (`"For P3 (queen sacrifice) and P5 (deflection sacrifice)…"`), so puzzle ordering is load-bearing for the prose.

## 6. age_band and unit confirmation

**Unit 5 name: confirmed.** `unit` is `{"number": 5, "name": "Advanced Tactics & Sacrifice"}` (showcase.json line 939). Note the literal ampersand `&`, not the word "and" — the `pacing_notes` prose spells it `"Advanced Tactics and Sacrifice"`, so the two do not match as strings.

**age_band: the stored value is `"10-12"` with an ASCII hyphen, but it does not render that way.** Every one of the four render sites substitutes an en dash:

- `app/js/ui/console.js:120` — `el(chain, 'li', '', \`${L.age_band.replace('-', '–')} yrs\`);` → renders `10–12 yrs`
- `app/js/ui/console.js:372` — `['ages', \`${L.age_band.replace('-', '–')}\`],` → prep sheet renders `10–12`
- `app/js/teach/main.js:323` and `app/js/teach/main.js:601` — same `.replace('-', '–')`

So on screen it is `10–12` (U+2013), never `10-12`. If a rebuild is asserting on rendered text, assert the en dash; if asserting on the JSON, assert the hyphen.

## 7. S042 shape summary — what changes if act 4 switches session

Act 4's console is currently wired to S042 at `app/js/main.js:37`: `const playPuzzle = buildConsole(document.getElementById('console'), showcase.data.S042);`

| | **S042** (current) | **S115** (target) |
|---|---|---|
| `title` | `"Fork Mastery"` | `"Three-Move Combinations"` |
| `subtitle` | `"Spot knight forks in busy, realistic positions — and watch the fork square."` | `"Calculate the entire forced line to checkmate before you play move one."` |
| `stage` | `{"number":2,"name":"Knight","color":"Blue"}` | `{"number":3,"name":"Bishop","color":"Purple"}` |
| `level` | `{"code":"2A","name":"Tactical Eyes"}` | `{"code":"3B","name":"The Strategic Player"}` |
| **unit name** | `1` / `"Double Attacks"` | `5` / `"Advanced Tactics & Sacrifice"` |
| **age band** | `"7-9"` → renders `7–9` | `"10-12"` → renders `10–12` |
| **planned minutes** | `60` | `60` (unchanged) |
| **timed minutes** | **47** | **42** |
| timed segments | 5 | 5 (unchanged) |
| segment minutes | 6 / 8 / 13 / 6 / 14 | 8 / 5 / 7 / 6 / 16 |
| practical activity | `"Castled-King Fork Hunt"`, 14 min | `"Calculate-Out-Loud Combination Game"`, 16 min |
| **puzzle count** | **8** | **8** (unchanged) |
| puzzle stars | `[1,1,1,2,2,2,3,3]` | `[1,1,2,2,2,3,3,3]` |
| `puzzles[0].solution.moves` length | 3 | 5 |
| **thinking routine** | `"THE SCAN"` — focus 121 chars | `"THE PLAN"` — focus **785 chars** |
| homework | 14 min | 15 min |
| prerequisites | `["S034","S035","S038"]` | `["S112","S113","S114"]` |
| `questions_to_ask` | 5 | 6 |
| `guided_examples` | 2 | 2 (unchanged) |
| `model_games` | `[]` | `[]` (unchanged) |
| coach_notes counts | 3/3/4/3/3 + string | identical |
| `session_type` | `"teaching"` | `"teaching"` (unchanged) |

S042's `thinking_routine.focus` verbatim: `"Scan for a knight check that ALSO hits a big piece — and make sure the forking square isn't defended (watch the g-pawn!)."` The two records have byte-identical top-level and puzzle-level key sets, so nothing structural breaks on the swap.

**Three places outside `main.js:37` carry S042-shaped constants and would go stale:**

1. `app/js/ui/bento.js:61` hardcodes S042's total: `el(c, 'p', 'c-n').innerHTML = '47<small>min in five parts</small>';` — S115's figure is 42.
2. `app/js/ui/bento.js:68` hardcodes S042's five segment minutes: `for (const seg of [['warm-up', 6], ['introduce', 8], ['explain', 13], ['discuss', 6], ['practice', 14]])` — S115's are 8 / 5 / 7 / 6 / 16. The inline bar widths divide by the literal `47` on line 74.
3. `app/js/gl/director.js:174` pins act 4's room light to Knight blue: `this._target.setHex(sessionAct > 0.5 ? HUES[1] : HUES[idx]);` with a comment at 170-172 saying "Act 4 shows session S042, which lives in the Knight stage… The light always agrees with the data on screen." S115 is stage 3 Bishop/Purple, i.e. `HUES[2]` = `0x9070ce`. Left as-is, the light would contradict the console.

Two smaller rendering facts that survive the swap unchanged but are worth knowing: the console shows only `puzzles[0]` (`console.js:39`, `const P = L.puzzles[0];`), so switching session swaps the on-screen board from `S042-P1` to `S115-P1` (`"6k1/p4ppp/2R5/1p4P1/3q4/7P/1N2r3/1K6 w - - 1 48"`, white to move, 5-move line). And the board caption prints `difficulty ${P.difficulty_stars} of 4` (`console.js:161`) while no puzzle in either session exceeds 3 stars — so it renders `difficulty 1 of 4` for both.

## HARD CONSTRAINTS
- showcase.json is generated, not hand-edited — C:\Users\MUS\Desktop\CurriculumWwebsitePrototype\app\data\showcase.json:2 — "$comment": "GENERATED by scripts/build-showcase.ts — do not edit. Run `npm run bundle:showcase`." Any change to S115's data must go through the build script, not the JSON.
- S115's thinking routine literal is UPPERCASE — app/data/showcase.json:990 — "current": "THE PLAN" — it is not "The Plan". app/js/ui/console.js:129 prints it verbatim (`el(r, 'p', 'r-v', L.thinking_routine.current)`), so screen text is THE PLAN.
- age_band is stored with an ASCII hyphen but never rendered with one — app/data/showcase.json:994 — "age_band": "10-12" — and app/js/ui/console.js:120 — el(chain, 'li', '', `${L.age_band.replace('-', '–')} yrs`) — renders 10–12 with U+2013. Same replacement at console.js:372, teach/main.js:323, teach/main.js:601.
- Unit 5's name uses an ampersand — app/data/showcase.json:939 — "name": "Advanced Tactics & Sacrifice" — while coach_notes.pacing_notes spells it 'Advanced Tactics and Sacrifice'; the two are not string-equal.
- Only four teaching_flow keys are ever timed by the console — app/js/ui/console.js:5-10 — const SEGMENTS = [['warm-up review','warmup_review'],['introduction','lesson_introduction'],['core explanation','core_explanation'],['guided discussion','guided_discussion']] — questions_to_ask and guided_examples carry no duration_min and are excluded by construction.
- A zero-minute segment would silently vanish — app/js/ui/console.js:100-101 — const m = L.teaching_flow?.[key]?.duration_min; if (!m) continue; — the guard is falsy, not null-checked.
- The plan-pane footer prints a COUNT, never the 42 — app/js/ui/console.js:148-149 — el(foot, 'span', '', `${L.estimated_duration_min} min planned`); el(foot, 'b', '', `${rows.length} timed parts`); — for S115 that renders '60 min planned' + '5 timed parts'.
- 42 appears only in the prep sheet — app/js/ui/console.js:373-374 — ['planned', `${L.estimated_duration_min} min`], ['timed', `${total} min`] — with total from app/js/ui/console.js:51 — const total = rows.reduce((a, r) => a + r[1], 0).
- Act 4's console session is a single call site — app/js/main.js:37 — const playPuzzle = buildConsole(document.getElementById('console'), showcase.data.S042);
- bento hardcodes S042's 47 minutes — app/js/ui/bento.js:61 — el(c, 'p', 'c-n').innerHTML = '47<small>min in five parts</small>'; — S115's timed total is 42.
- bento hardcodes S042's five segment minutes — app/js/ui/bento.js:68 — for (const seg of [['warm-up', 6], ['introduce', 8], ['explain', 13], ['discuss', 6], ['practice', 14]]) { — S115's are 8/5/7/6/16, and line 74 divides bar widths by the literal 47.
- Act 4's room light is pinned to Knight blue — app/js/gl/director.js:174 — this._target.setHex(sessionAct > 0.5 ? HUES[1] : HUES[idx]); — with HUES defined at director.js:33 as [0x3fa57a, 0x4a8bd0, 0x9070ce, 0xd2604b, 0xc9a227]; S115 is stage 3 Bishop/Purple = HUES[2] = 0x9070ce.
- The console renders only the first puzzle — app/js/ui/console.js:39 — const P = L.puzzles[0];
- The board caption over-states the star ceiling — app/js/ui/console.js:161 — el(cap, 'b', '', `difficulty ${P.difficulty_stars} of 4`); — no S115 or S042 puzzle exceeds 3 stars.
- 'POSITION 4 OF 8' maps 1:1 onto puzzles[3] — app/js/teach/segments.js:74 — beats: lesson.puzzles.map((p) => ({ ... })) — with the label from app/js/teach/main.js:353 — ` · position ${S.beat + 1} of ${sg.beats.length}`.
- Only three sessions are free/bundled — app/js/teach/main.js:15 — const FREE = ['S001', 'S042', 'S115']; — and app/js/teach/main.js:49 falls back to S042: S.id = FREE.includes(wanted) ? wanted : 'S042';
- homework time uses a different key name and is excluded from every duration_min sum — app/data/showcase.json:1394 — "estimated_time_min": 15
- S115 has no model games — app/data/showcase.json:1357 — "model_games": [], — and core_explanation.demonstrations is likewise empty, so app/js/teach/segments.js:47 (beats: orOne((core.demonstrations || []).map(...))) falls through to its one-empty-beat default.

## UNKNOWNS
- Whether the string "60 MIN PLANNED · 42 TIMED" exists anywhere as authored copy. I grepped app/js, app/css and the HTML for 'PLANNED', 'TIMED' and 'MIN PLANNED' and found no such combined literal — only the separate '{n} min planned' / '{n} timed parts' footer and the prep sheet's 'planned {n} min' / 'timed {n} min' pair. It may be a spec/design-comp phrase rather than shipped text, or it may live in a file type I did not search (e.g. a .md or design asset).
- Whether the CSS uppercases those labels. I confirmed the JS emits lowercase 'min planned' / 'timed parts', but did not read app/css/acts.css or annot.css to see whether a text-transform makes them render as caps, which is what your quoted string implies.
- Whether the intended switch is act 4's console only (app/js/main.js:37) or also the teach page's default session (app/js/teach/main.js:49, which falls back to S042). The two are independent code paths and I was not told which you mean.
- Whether the bento b-console card and act 4's console appear on the same scroll surface, i.e. whether the hardcoded 47 at bento.js:61 would be visibly adjacent to a 42 after the switch. I read bento.js but did not trace where buildBento is mounted relative to the console.
- Whether app/js/ui/compare.js:370's warm-up comparison across S001/S042/S115 hardcodes S042 minutes too. The comment mentions 'against 50- and 60-minute slots' but I did not read the surrounding code.
- Whether the promotion SAN 'hxg8=Q#' in S115-P8 replays cleanly through the bundled Chess implementation. The console never reaches puzzle index 7, but the teach page does; I did not execute the move list against the engine.
- Whether anything validates that estimated_duration_min (60) should equal the sum of duration_min (42). The 18-minute gap is consistent across all three free sessions (S001: 50 vs 44; S042: 60 vs 47), so it looks intentional, but I found no schema or test asserting the relationship.
- The .audit5/snap and .archive-2026-09-01 copies contain parallel versions of console.js, bento.js and teach/main.js. I did not diff them against app/js, so I cannot say whether they are stale snapshots or live inputs to a build.